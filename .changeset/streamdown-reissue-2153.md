---
"@lofcz/streamdown": patch
---

Re-issue the 2.15.2 release: its registry metadata published but the tarball blob never materialized (persistent 404), so this version re-ships the same content under a new number.
