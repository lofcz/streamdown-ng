---
title: Internationalization
description: Localize UI labels and set text direction for RTL and mixed-script content.
type: reference
summary: Localize UI labels and configure text direction (dir) for RTL content.
prerequisites:
  - /docs/getting-started
related:
  - /docs/configuration
  - /docs/interactivity
---

Streamdown ships with English labels for all interactive controls — copy buttons, download menus, link modals, and more. Override any label via the `translations` prop.

## Basic usage

Pass a partial translations object to replace specific labels:

```tsx title="app/page.tsx"
<Streamdown
  translations={{
    copyCode: "Copiar código",
    copyDiagram: "Copiar diagrama",
    close: "Cerrar",
    openLink: "Abrir enlace",
    downloadImage: "Descargar imagen",
  }}
>
  {markdown}
</Streamdown>
```

## Available translation keys

### Code block

<TypeTable
  type={{
    copyCode: {
      description: "Tooltip for the code copy button",
      type: "string",
      default: '"Copy Code"',
    },
    downloadFile: {
      description: "Tooltip for the file download button",
      type: "string",
      default: '"Download file"',
    },
  }}
/>

### Diagrams and models (shared)

These labels are shared by Mermaid, PlantUML, and OpenSCAD controls.

<TypeTable
  type={{
    copyDiagram: {
      description: "Tooltip for the Mermaid and PlantUML copy buttons",
      type: "string",
      default: '"Copy diagram"',
    },
    copyModel: {
      description: "Tooltip for the OpenSCAD copy button",
      type: "string",
      default: '"Copy model"',
    },
    diagramLoading: {
      description: "Loading text while a diagram is rendering",
      type: "string",
      default: '"Loading diagram..."',
    },
    showCode: {
      description: "Toggle that reveals source after a render error",
      type: "string",
      default: '"Show Code"',
    },
    viewFullscreen: {
      description: "Open fullscreen overlay (diagrams, tables, images)",
      type: "string",
      default: '"View fullscreen"',
    },
    exitFullscreen: {
      description: "Close fullscreen overlay",
      type: "string",
      default: '"Exit fullscreen"',
    },
    zoomIn: {
      description: "Pan/zoom control: zoom in",
      type: "string",
      default: '"Zoom in"',
    },
    zoomOut: {
      description: "Pan/zoom control: zoom out",
      type: "string",
      default: '"Zoom out"',
    },
    resetView: {
      description: "Pan/zoom control: reset zoom and pan",
      type: "string",
      default: '"Reset zoom and pan"',
    },
  }}
/>

### Mermaid diagrams

<TypeTable
  type={{
    mermaidChart: {
      description: "Accessible name for the rendered Mermaid SVG",
      type: "string",
      default: '"Mermaid chart"',
    },
    downloadDiagram: {
      description: "Label for the diagram download button",
      type: "string",
      default: '"Download diagram"',
    },
    downloadDiagramAsSvg: {
      description: "Download diagram as SVG option",
      type: "string",
      default: '"Download diagram as SVG"',
    },
    downloadDiagramAsPng: {
      description: "Download diagram as PNG option",
      type: "string",
      default: '"Download diagram as PNG"',
    },
    downloadDiagramAsMmd: {
      description: "Download diagram as MMD option",
      type: "string",
      default: '"Download diagram as MMD"',
    },
    mermaidFormatSvg: {
      description: "Short format label for SVG",
      type: "string",
      default: '"SVG"',
    },
    mermaidFormatPng: {
      description: "Short format label for PNG",
      type: "string",
      default: '"PNG"',
    },
    mermaidFormatMmd: {
      description: "Short format label for MMD",
      type: "string",
      default: '"MMD"',
    },
    mermaidErrorLabel: {
      description: "Prefix shown before a Mermaid render error",
      type: "string",
      default: '"Mermaid Error"',
    },
    mermaidPluginMissing: {
      description: "Error when the Mermaid plugin is not installed",
      type: "string",
      default: '"Mermaid plugin not available. Please add the mermaid plugin to enable diagram rendering."',
    },
    mermaidRenderFailed: {
      description: "Fallback when Mermaid rendering fails without an error message",
      type: "string",
      default: '"Failed to render Mermaid chart"',
    },
  }}
/>

### Vega charts

<TypeTable
  type={{
    vegaChart: {
      description: "Accessible name for the rendered Vega SVG",
      type: "string",
      default: '"Vega chart"',
    },
    downloadDiagramAsJson: {
      description: "Download diagram as JSON option",
      type: "string",
      default: '"Download diagram as JSON"',
    },
    vegaFormatJson: {
      description: "Short format label for Vega source",
      type: "string",
      default: '"JSON"',
    },
    vegaErrorLabel: {
      description: "Prefix shown before a Vega render error",
      type: "string",
      default: '"Vega Error"',
    },
    vegaPluginMissing: {
      description: "Error when the Vega plugin is not installed",
      type: "string",
      default: '"Vega plugin not available. Please add the vega plugin to enable chart rendering."',
    },
    vegaRenderFailed: {
      description: "Fallback when Vega rendering fails without an error message",
      type: "string",
      default: '"Failed to render Vega chart"',
    },
  }}
/>

### SMILES structures

<TypeTable
  type={{
    smilesChart: {
      description: "Accessible name for the rendered chemical structure SVG",
      type: "string",
      default: '"Chemical structure"',
    },
    downloadDiagramAsSmi: {
      description: "Download diagram as SMILES option",
      type: "string",
      default: '"Download diagram as SMILES"',
    },
    smilesFormatSmi: {
      description: "Short format label for SMILES source",
      type: "string",
      default: '"SMI"',
    },
    smilesErrorLabel: {
      description: "Prefix shown before a SMILES render error",
      type: "string",
      default: '"SMILES Error"',
    },
    smilesPluginMissing: {
      description: "Error when the SMILES plugin is not installed",
      type: "string",
      default: '"SMILES plugin not available. Please add the smiles plugin to enable chemical structure rendering."',
    },
    smilesRenderFailed: {
      description: "Fallback when SMILES rendering fails without an error message",
      type: "string",
      default: '"Failed to render SMILES structure"',
    },
  }}
/>

### PlantUML diagrams

<TypeTable
  type={{
    plantumlChart: {
      description: "Accessible name for the rendered PlantUML SVG",
      type: "string",
      default: '"PlantUML chart"',
    },
    downloadDiagramAsPuml: {
      description: "Download diagram as PlantUML option",
      type: "string",
      default: '"Download diagram as PlantUML"',
    },
    plantumlFormatSvg: {
      description: "Short format label for SVG",
      type: "string",
      default: '"SVG"',
    },
    plantumlFormatPng: {
      description: "Short format label for PNG",
      type: "string",
      default: '"PNG"',
    },
    plantumlFormatPuml: {
      description: "Short format label for PlantUML source",
      type: "string",
      default: '"PUML"',
    },
    plantumlErrorLabel: {
      description: "Prefix shown before a PlantUML render error",
      type: "string",
      default: '"PlantUML Error"',
    },
    plantumlPluginMissing: {
      description: "Error when the PlantUML plugin is not installed",
      type: "string",
      default: '"PlantUML plugin not available. Please add the plantuml plugin to enable diagram rendering."',
    },
    plantumlRenderFailed: {
      description: "Fallback when PlantUML rendering fails without an error message",
      type: "string",
      default: '"Failed to render PlantUML chart"',
    },
  }}
/>

### OpenSCAD models

<TypeTable
  type={{
    openscadModel: {
      description: "Accessible name for the 3D model viewer",
      type: "string",
      default: '"OpenSCAD model"',
    },
    downloadModel: {
      description: "Label for the model download button",
      type: "string",
      default: '"Download model"',
    },
    downloadModelAsScad: {
      description: "Download model as SCAD option",
      type: "string",
      default: '"Download model as SCAD"',
    },
    downloadModelAsStl: {
      description: "Download model as STL option",
      type: "string",
      default: '"Download model as STL"',
    },
    downloadModelAs3mf: {
      description: "Download model as 3MF option",
      type: "string",
      default: '"Download model as 3MF"',
    },
    openscadFormatScad: {
      description: "Short format label for SCAD",
      type: "string",
      default: '"SCAD"',
    },
    openscadFormatStl: {
      description: "Short format label for STL",
      type: "string",
      default: '"STL"',
    },
    openscadFormat3mf: {
      description: "Short format label for 3MF",
      type: "string",
      default: '"3MF"',
    },
    openscadLoading: {
      description: "Loading text while the OpenSCAD engine is compiling",
      type: "string",
      default: '"Loading OpenSCAD engine and rendering model..."',
    },
    openscadWriting: {
      description: "Placeholder while the OpenSCAD fence is still streaming",
      type: "string",
      default: '"Waiting for the model code..."',
    },
    openscadErrorLabel: {
      description: "Prefix shown before an OpenSCAD render error",
      type: "string",
      default: '"OpenSCAD Error"',
    },
    openscadPluginMissing: {
      description: "Error when the OpenSCAD plugin is not installed",
      type: "string",
      default: '"OpenSCAD plugin not available. Please add the openscad plugin to enable model rendering."',
    },
    openscadRenderFailed: {
      description: "Fallback when OpenSCAD rendering fails without an error message",
      type: "string",
      default: '"Failed to render model"',
    },
  }}
/>

### GitHub alerts

<TypeTable
  type={{
    alertNote: {
      description: "Title for `[!NOTE]` alerts",
      type: "string",
      default: '"Note"',
    },
    alertTip: {
      description: "Title for `[!TIP]` alerts",
      type: "string",
      default: '"Tip"',
    },
    alertImportant: {
      description: "Title for `[!IMPORTANT]` alerts",
      type: "string",
      default: '"Important"',
    },
    alertWarning: {
      description: "Title for `[!WARNING]` alerts",
      type: "string",
      default: '"Warning"',
    },
    alertCaution: {
      description: "Title for `[!CAUTION]` alerts",
      type: "string",
      default: '"Caution"',
    },
  }}
/>

### Table

<TypeTable
  type={{
    copyTable: {
      description: "Label for the table copy button",
      type: "string",
      default: '"Copy table"',
    },
    copyTableAsMarkdown: {
      description: "Copy as Markdown option",
      type: "string",
      default: '"Copy table as Markdown"',
    },
    copyTableAsCsv: {
      description: "Copy as CSV option",
      type: "string",
      default: '"Copy table as CSV"',
    },
    copyTableAsTsv: {
      description: "Copy as TSV option",
      type: "string",
      default: '"Copy table as TSV"',
    },
    downloadTable: {
      description: "Label for the table download button",
      type: "string",
      default: '"Download table"',
    },
    downloadTableAsCsv: {
      description: "Download table as CSV option",
      type: "string",
      default: '"Download table as CSV"',
    },
    downloadTableAsMarkdown: {
      description: "Download table as Markdown option",
      type: "string",
      default: '"Download table as Markdown"',
    },
    tableFormatMarkdown: {
      description: "Short format label for Markdown",
      type: "string",
      default: '"Markdown"',
    },
    tableFormatCsv: {
      description: "Short format label for CSV",
      type: "string",
      default: '"CSV"',
    },
    tableFormatTsv: {
      description: "Short format label for TSV",
      type: "string",
      default: '"TSV"',
    },
  }}
/>

### Image

<TypeTable
  type={{
    imageNotAvailable: {
      description: "Fallback text for broken images",
      type: "string",
      default: '"Image not available"',
    },
    downloadImage: {
      description: "Tooltip for the image download button",
      type: "string",
      default: '"Download image"',
    },
  }}
/>

### Link modal

<TypeTable
  type={{
    openExternalLink: {
      description: "Modal title for external links",
      type: "string",
      default: '"Open external link?"',
    },
    externalLinkWarning: {
      description: "Warning text in the link modal",
      type: "string",
      default: '"You\'re about to visit an external website."',
    },
    close: {
      description: "Close button label",
      type: "string",
      default: '"Close"',
    },
    copyLink: {
      description: "Copy link button label",
      type: "string",
      default: '"Copy link"',
    },
    copied: {
      description: "Label shown after copying",
      type: "string",
      default: '"Copied"',
    },
    openLink: {
      description: "Open link button label",
      type: "string",
      default: '"Open link"',
    },
  }}
/>

## Partial overrides

The `translations` prop accepts `Partial<StreamdownTranslations>`. Any key you omit falls back to the built-in English default:

```tsx title="app/page.tsx"
// Only override what you need — everything else stays English
<Streamdown translations={{ copyCode: "コピー", close: "閉じる" }}>
  {markdown}
</Streamdown>
```

## Access translations in custom components

Use the `useTranslations` hook inside custom components to read the active translations:

```tsx title="app/page.tsx"
import { useTranslations } from "streamdown";

function CustomCodeBlock({ children }) {
  const translations = useTranslations();

  return (
    <div>
      <button>{translations.copyCode}</button>
      <pre><code>{children}</code></pre>
    </div>
  );
}
```

## Text direction

For right-to-left languages or mixed-script Markdown, set the `dir` prop. Prefer `dir="auto"` so each block gets its own base direction:

```tsx title="app/page.tsx"
<Streamdown dir="auto">
  {markdown}
</Streamdown>
```

In static mode, Streamdown keeps the document as one Markdown parse (so footnotes and references still resolve) and assigns `dir` per heading, paragraph, list item, quote, and table cell. Code blocks stay LTR. Detection uses a majority count of strong Unicode letters, with first-strong as the tie-breaker.

See [Configuration → Text direction](/docs/configuration#text-direction-dir) for the full behavior matrix.

## Exported types and values

```tsx
import type { StreamdownTranslations } from "streamdown";
import { defaultTranslations, useTranslations } from "streamdown";
```

- `StreamdownTranslations` — interface with every built-in UI label
- `defaultTranslations` — the built-in English defaults
- `useTranslations()` — React hook returning the active translations object
